const {
  Proskomma,
  lexingRegexes,
  resolvers,
  typeDefs,
  blocksSpecUtils,
} = require('./src/index.cjs');

module.exports = {
  Proskomma, lexingRegexes, blocksSpecUtils, resolvers, typeDefs,
};
